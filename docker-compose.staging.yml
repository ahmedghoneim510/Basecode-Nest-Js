services:
  app:
    env_file:
      - .env.staging
    environment:
      - POSTGRES_DB=app_staging

  postgres:
    environment:
      POSTGRES_USER: app_user
      POSTGRES_PASSWORD: StAgInG_pAsS_2024
      POSTGRES_DB: app_staging
