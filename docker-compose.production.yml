services:
  app:
    env_file:
      - .env.production
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"

  postgres:
    environment:
      POSTGRES_USER: app_user
      POSTGRES_PASSWORD: PR0D_S3CUR3_P@SS
      POSTGRES_DB: app_production

  redis:
    command: redis-server --requirepass redis-prod-password
